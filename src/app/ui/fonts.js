import { Cinzel, Nunito_Sans } from 'next/font/google'

export const cinzel = Cinzel({ subsets: ['latin'], variable: '--font-cinzel', display: 'swap' })
export const nunito = Nunito_Sans({ subsets: ['latin'], variable: '--font-nunito', display: 'swap' })